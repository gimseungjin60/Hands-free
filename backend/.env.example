# OpenAI API 키 (브라우저에 노출 금지 — 서버 전용)
OPENAI_API_KEY=여기에_키_입력

# 모델 (선택 — 미설정 시 기본값)
# STT_MODEL=whisper-1
# CHAT_MODEL=gpt-4o-mini
# TTS_MODEL=tts-1
# TTS_VOICE=nova

# 보드 데이터 교체 시 (선택)
# BOARD_FILE=data/board_atmega128.json
